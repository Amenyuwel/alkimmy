services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: base
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "3000:3000"
    environment:
      - VITE_PORT=3000
    command: pnpm dev --host 0.0.0.0
  